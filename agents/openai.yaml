interface:
  display_name: "AI Mapper Agent"
  short_description: "Map China-relevant AI software with evidence"
  default_prompt: "Use $ai-mapper-agent to run a source-backed local AI project map."
policy:
  allow_implicit_invocation: true
